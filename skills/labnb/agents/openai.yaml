interface:
  display_name: "Lab Notebook"
  short_description: "Track ideas and experiments safely across repos"
  default_prompt: "Use $labnb to summarize prior notebook entries, capture ideas, and run isolated experiments safely outside this repo."

policy:
  allow_implicit_invocation: true
