# structsense — for developing/validating the skill itself. Not needed to run it.
#
#     pip install -r requirements-dev.txt

# rdflib and pyshacl are core requirements now (requirements.txt): the Turtle
# is the deliverable, and scripts/validate_ttl.py gates it.

# Read the NBDC catalog workbook with a dataframe for ad-hoc inspection.
pandas>=2.0,<3
pyarrow>=15.0
